{% if items.next %}
  <ul{{ attributes }}>
    <li class="pager__item">
      <a class="button" href="{{ items.next.href }}" title="{{ 'Go to next page'|t }}"
         rel="next">{{ options.mypager.button_text }}</a>
    </li>
  </ul>
{% endif %}


